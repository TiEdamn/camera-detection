import React from 'react';
import Start from './Start';
import Reset from './Reset';
import './App.scss';

export default () => {
    return (
        <React.Fragment>
            <Start />
            <Reset />
        </React.Fragment>
    );
};
